node_modules/
.wrangler/
.dev.vars
.env
*.log
dist/
!packages/cf-ai-gateway/dist/
.DS_Store

# build/ 全為生成物（Fuse 索引、manifest、Vectorize 同步用的 .sql / .ndjson），不入版控
build/
.claude/

report.md

# local CAG memory prototype checkpoints (JSONL)
local/cag-memories/
local/cag-compare/
# local offline CAG section cosine index (Ollama embeddings)
local/section-index.json
